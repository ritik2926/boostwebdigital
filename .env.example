# Resend API key — used by src/app/api/contact/route.ts and
# src/app/api/newsletter/subscribe/route.ts to send email. Get one at
# https://resend.com (free tier is enough to start). Without this set, the
# contact form fails loudly with a 503; the newsletter route logs and skips
# sending rather than failing the signup itself.
RESEND_API_KEY=

# Neon Postgres connection string — used ONLY by src/lib/db.ts, which may
# only be imported from files under src/app/api/. Backs the newsletter
# subscribers/sent_posts tables (src/lib/db.ts documents the schema). Get
# one from your Neon project's dashboard.
DATABASE_URL=

# Upstash Redis REST credentials — used by src/lib/rate-limit.ts to rate-
# limit newsletter signups (3 per IP per hour), via plain REST calls, no
# SDK. Get these from your Upstash Redis database's dashboard. If unset,
# rate limiting is skipped (fails open) rather than blocking real signups.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
